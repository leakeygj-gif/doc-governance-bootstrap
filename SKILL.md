---
name: doc-governance-bootstrap
description: 一次性把"文档治理工作流"搭进当前代码仓库——按验证复杂度决定要不要立 Task（而非按改动规模）、Plan+Report 成对且 Report 定稿后数据不可改、git pre-commit 钩子强制源码改动必须配交接日志、规格文档以参数来源与设计意图减少重复维护、外加一份可随时手动执行的文档巡检清单。规则写入 AGENTS.md（跨工具通用，Codex 等直接读取）并让 CLAUDE.md 通过 @AGENTS.md 导入，不是 Claude Code 独占。当用户提到想让 AI 改代码后不要忘记更新文档、防止 spec/规格文档和代码脱节、想要 changelog/HANDOFF 强制记录、想设计 Task/Plan/Report 或类似的任务管理流程、想加 git hook 强制提交带文档、抱怨文档"漂移""过时""没人维护"、想让不同 AI 编程工具（Claude Code、Codex 等）遵守同一套项目规则、或想把某个项目已经在用的文档规范复制到新项目时，都应该主动使用本 skill——即使用户没有直接说"skill"或"文档治理"这几个字。这是**一次性初始化**动作：调用一次把结构和规则种进目标仓库后，日常的立项判断、日志记录、巡检执行都由被写入的 AGENTS.md 和 docs/WORKFLOW.md 在后续对话里自然发挥作用，不需要每次都重新调用本 skill。
---

# 文档治理体系初始化

## 这是什么

把一套经过真实项目反复打磨的文档治理体系，种进当前这个代码仓库：

- `docs/specs/` — 系统规格（参数来源与设计意图，必要值引用权威来源）
- `docs/tasks/` — Plan + Report 成对的任务文档（按验证复杂度决定要不要立项）
- `docs/refs/` — 外部参考资料索引
- `docs/HANDOFF.md` — 追加式交接日志
- `docs/WORKFLOW.md` — 协作流程 + 文档巡检清单
- `.githooks/pre-commit` — 源码改动必须配日志的机械强制
- `tools/doc_symbols.py` — 规格文档符号词法自检脚本
- `tools/task_pairs.py` — Task 同目录配对、完整前缀/显式关联、状态与历史豁免检查
- `AGENTS.md` 里写四条规则（任务立项 / 分叉执行 / 交接日志 / 规格同步），`CLAUDE.md` 用 `@AGENTS.md` 导入同一份内容

**为什么规则写进 `AGENTS.md` 而不是 `CLAUDE.md`**：`AGENTS.md` 是跨工具的中性约定，Codex 等工具会直接读取；Claude Code 目前**不会**自动读 `AGENTS.md`，但官方支持在 `CLAUDE.md` 顶部写一行 `@AGENTS.md` 做机械导入（读 `CLAUDE.md` 时会把 `AGENTS.md` 内容展开进去）。这样规则只有一份，两类工具都能拿到同一套——不是"写两份、指望它们保持一致"，也不是像"文件 A 里留一句『见文件 B』"这种软提示，是官方支持的机械展开机制。

**这些规则不是拍脑袋定的**，每一条背后都有一个真实项目里发生过的具体失败场景。在向用户解释、或者用户想调整某条规则时，先读 `references/methodology.md`——里面详细说明了每条规则解决的是什么问题、删掉它会重新打开哪个口子。不要凭空辩护或凭空让步。

## 执行前必须先确认：这是持久化写入

本 skill 会在目标仓库创建/修改多个文件，属于持久化变更。**动手写文件之前，先把下面"要做什么"的计划讲给用户听，等待明确确认**——不要在完成访谈问题之后直接开始写文件，除非用户已经清楚表示"你决定就行"这类授权。

已有项目的版本比较或升级请求：先读 [只读升级预览](references/upgrade-preview.md)，区分开发仓库、安装副本与项目生成文件，输出差异和保留项；不要重新初始化或自动同步。用户已确认的项目约定优先，包括明确不建立 CLAUDE.md 的决定。下文导入要求适用于采用 Claude Code 导入方案的项目。

## 步骤 1：访谈（3 个问题，不要跳过，也不要多问）

在开始创建任何文件之前，问用户：

1. **源码目录和文件类型**——比如 `src/**.py`、`lib/**.ts`、`(cmd|internal|pkg)/**.go`。这决定 `.githooks/pre-commit` 里源码匹配规则怎么写。
2. **文档用什么语言**——默认建议跟随当前对话使用的语言，但要让用户确认，因为这是个持久决定。生成的说明用这个语言；机器标记保留原样（见下方"关于语言"）。
3. **要不要设置称呼规则**（回复时怎么称呼用户，或者团队有没有类似的沟通约定）——**可选**，用户说不需要就跳过，不要强加。

不要多问。分支协调、CI 集成这类问题**不需要访谈**——用不到的时候本体系本来就不依赖它们，用得到的时候用户会自己提。

## 步骤 2：探测目标仓库现状，避免覆盖——但"已存在"不等于"可以直接跳过"

在写任何文件之前，检查：

- 检查 `docs/specs/`、`docs/tasks/`、`docs/refs/` 内文件、`docs/WORKFLOW.md`、`.githooks/pre-commit`、`tools/doc_symbols.py`、`tools/task_pairs.py`。**已存在的不自动覆盖，但必须读内容检查兼容性**：立项判据、报告配对与状态、review 权限、规格值约定、日志方向、hook 匹配范围和脚本命令是否与待写规则一致。列出兼容复用、冲突待处理和缺失项；未解决冲突前，不把入口规则更新到与保留模板不兼容的版本，也不得报告完整安装成功。需要迁移时提出具体方案另行确认，本 skill 不做通用自动升级。
- 读取 `git config --show-origin --get core.hooksPath`，检查已有 hook 及执行权限。已有其他 hooksPath 或 hook 内容时，保留现状并确认整合方案，不能直接覆盖路径或禁用已有检查。
- `AGENTS.md` 和 `CLAUDE.md` 是否存在——见下方"写入 AGENTS.md，并确保 CLAUDE.md 导入它"，这一步不是简单的"存在就追加、不存在就新建"，需要按概念替换掉同名/同类的旧规则，**旧规则可能在 AGENTS.md 里，也可能直接写在 CLAUDE.md 里**（本 skill 早期版本是直接写进 CLAUDE.md 的，如果目标项目是用那个版本装的，四条规则会在 CLAUDE.md 里而不是 AGENTS.md 里——这也要一并找出来处理，不是新场景，处理方式相同：按概念替换）。
- `.gitignore` 是否已经排除了常见的临时/生成文件——不需要主动改，除非用户要求。

**`docs/HANDOFF.md` 需要单独判断，不能简单"存在就跳过"。** 如果它已经存在，先检查是否匹配我们的约定：文件里有没有那句标记注释（`<!-- 在这条注释下方追加新记录，最新的在最上面 -->`）？条目是否遵循有 Task 简版、无 Task 五段结构？

- **匹配**（说明这个项目之前已经装过这套体系，或者巧合地采用了同样的约定）→ 跳过，不覆盖，正常汇报。
- **不匹配**（文件存在，但格式、字段、追加方向都是这个项目自己原有的习惯——常见情况是"旧的在前、新的在后"地按时间顺序往下追加，条目是自由格式而非五段结构）→ **不要静默跳过**。这种情况下，`AGENTS.md` 里新写的"交接日志规则"会要求以后的记录"插最前面、按有无 Task 分档"，而这份文件几个月来一直是"追加在后面、自由格式"——两者会直接冲突，而且没有任何标记会提醒后续的人格式已经变了。**必须停下来，把这个冲突明确讲给用户听，问清楚要怎么处理**，给出选项参考：
  1. **合并进同一个 `docs/HANDOFF.md`，用统一的分界标记隔开**（推荐默认）：把旧内容原样接到新模板下方，中间插入这行固定文字（逐字复用，方便后续脚本识别）：
     ```
     ---

     <!-- 归档分界：以下内容归档自旧格式历史记录，不适用上方"新记录插最上面/五段结构"的约定 -->

     ## 历史记录（<旧文件的日期范围>，旧格式，仅供追溯，不再追加）

     > 这段以下是本项目在装这套文档体系之前自己维护的开发日志……
     ```
     旧内容本身**不做任何整理或重写**，原样接上去。**不要单独另存一个 `docs/HANDOFF_历史.md` 之类的归档文件**——两份文件容易让人以为要分别维护，一份文件配一条清晰的分界线，找起来和确认边界都更直接。`docs/WORKFLOW.md` 的 A4 巡检脚本认的就是这行分界注释，用来在合并后正确停止日期序检查，见该文件里的说明。
  2. 保留旧文件、旧习惯不变，`AGENTS.md` 的交接日志规则相应调整措辞（不强制"插最前面"和五段结构），迁就这个项目已有的方式；
  3. 用户自己另有安排。
  不要替用户做这个决定——这类"日志往哪个方向追加"的选择一旦定下来，后面所有记录都会照着做，改起来成本很高，必须让用户明确拍板。

同样，**检查目标仓库有没有类似 `docs/PROJECT_STATUS.md` 这种"覆盖式项目状态快照"文件**（不一定叫这个名字，特征是：每次改动后整份重写、试图汇总"当前进度"）。如果有，明确告诉用户：这类文件在我们的方法论里被认为是冗余的（内容会从 `docs/tasks/README.md` 的项目登记表派生，另开一份必然漂移，真实项目里出现过 42 条风险记录只有极少数还成立的情况）——但**是否要在这个项目里废弃它，由用户决定**，不要擅自删除或不再维护，只是要明确提出来，不要假装没看见。

## 步骤 3：生成文件

`assets/templates/` 下的文件是内容基础，按下面的方式逐个处理：

| 模板 | 目标路径 | 处理方式 |
|---|---|---|
| `specs-README.md` | `docs/specs/README.md` | 直接复制（内容已通用） |
| `tasks-README.md` | `docs/tasks/README.md` | 直接复制（登记表为空，项目从 P01 起） |
| `refs-README.md` | `docs/refs/README.md` | 直接复制（清单为空） |
| `HANDOFF.md` | `docs/HANDOFF.md` | 直接复制（只有模板头，无历史记录——**不要编造历史条目**，即使仓库已有很长的 git 提交历史，本文件也应该从空白开始，往前追溯是考古工作，不在本次范围内） |
| `WORKFLOW.md` | `docs/WORKFLOW.md` | 直接复制 |
| `pre-commit` | `.githooks/pre-commit` | 把 `{{SRC_GLOB}}` 替换成步骤 1 核对的源码匹配正则，按实际语言纳入 `docs/tasks/` 内的任务脚本，不将整个文档目录排除；复制后 `chmod +x` |
| `doc_symbols.py` | `tools/doc_symbols.py` | 复制；始终按步骤 1 的实际源码目录与文件类型调整 `SRC_DIR_CANDIDATES` 和 `SRC_EXTENSIONS`，即使目录属于内置候选也要核对，避免漏扫语言或目录 |
| `task_pairs.py` | `tools/task_pairs.py` | 复制；同目录 plan/report 默认配对，兼容阶段及历史完整前缀或显式关联，CLI 与有来源的历史豁免见 WORKFLOW A5 |
| `AGENTS-rules-block.md` | 追加进 `AGENTS.md` | 见下方"写入 AGENTS.md，并确保 CLAUDE.md 导入它" |
| （无模板，一行文本） | `CLAUDE.md` 顶部 | 仅采用 Claude Code 导入方案时，确保有 `@AGENTS.md` 这一行——`CLAUDE.md` 不存在就新建一个只含这一行的文件；已存在但没有这行就加在最前面；已经有就不重复加 |

新任务资料规则由 `tasks-README.md` 提供：新任务集中到 `docs/tasks/P<编号>_<日期>_<任务名>/`，内含 plan/report 与按需创建的专用脚本、清单、结果；少资料不强制子分类。报告资料表链接清单。初始化不创建空任务目录或资料占位文件；实际复用后再考虑提取公共工具；产品代码留源码，大型原数据和已有共享脚本可原位引用，历史不批量迁移。

### 关于语言

`assets/templates/` 下的内容是中文写就的（这是这套方法论被验证、打磨的语言）。**如果用户选择的目标语言不是中文，你需要翻译说明正文**——保留结构、逻辑和例子不变，不要在翻译过程中简化或丢失某条规则的推理过程（尤其是"为什么"部分，那是最容易被翻译时顺手精简掉、但恰恰最不该丢的部分）。翻译前建议先读一遍 `references/methodology.md`，确保理解每条规则的意图，翻译才不会跑偏。

机器契约不翻译：HANDOFF 两条固定注释、阶段/复测报告关联行 `**对应计划**：[标签](plan.md)` 的字段和链接语法、Task 的 `**状态**：` 字段与状态值（草拟中 / 已确认 / 执行中 / 已完成 / 搁置 / 已废弃）整行保留原样；`**验收边界**` 字段名也保留，说明可翻译，译文另起一行；文件名、CLI 参数和代码标识符也保留。这样翻译后的文档仍能被巡检器识别，不另建翻译系统。

### 写入 AGENTS.md，并确保 CLAUDE.md 导入它——这一步最容易做得"看起来完成了但其实没完成"，务必逐条核对

**先搜索，不要直接追加。** `AGENTS-rules-block.md` 里有四条规则（任务立项 / 分叉执行 / 交接日志 / 规格同步）。目标项目里**很可能已经存在这四条规则的旧版本**——尤其如果这个项目之前手工搭过一套类似但更早期的文档习惯，或者是用本 skill 更早的版本装的（那时规则是直接写进 `CLAUDE.md` 的，还没有 `AGENTS.md` 这一层）。**旧版本可能在 `AGENTS.md` 里，也可能在 `CLAUDE.md` 里，两个文件都要搜**，不一定叫一样的名字，但按概念逐条去找：

- 有没有一条规则在说"改动规模多大/耗时多久就要写计划文档"（哪怕叫"计划落盘规则"）？→ 这是"任务立项规则"的旧版本，**替换掉它**，不要在它旁边再追加一条新的，那样会产生两条互相矛盾的规则同时生效。
- 有没有一条规则在说"完成一段**实质性**改动后要更新日志"，尤其如果它同时提到要维护一份 `PROJECT_STATUS.md` 或类似的"覆盖式状态文档"？→ 这是"交接日志规则"的旧版本，**这条最容易被漏改，务必单独确认它被替换了**——"实质性"是主观词，旧规则要求维护的状态文档正是我们这套体系刻意不设的东西（状态应该从 `docs/tasks/` 的登记表派生，另开一份就会漂移）。
- 类似地核对"分叉执行规则""规格同步规则"是否已有旧版本。

**以下替换与迁移仅针对已确认采用的新治理规则。** 用户明确保留的原约定（包括 `PROJECT_STATUS.md` 的维护要求）继续保留，并同步调整生成模板以保持兼容；同一 bullet 混有治理规则和保留约定时先拆分，只替换治理部分，不删除保留内容。

**替换，不是并存，而且要落在正确的文件里。**

- 若旧规则在 `AGENTS.md` 里（或者本来就没有 `AGENTS.md`，是全新安装）→ 在 `AGENTS.md` 里原地替换 / 新增，逻辑和之前完全一样。
- 若旧规则在 `CLAUDE.md` 里（老版本装的）→ **这是一次迁移，不是简单替换**：把这四条规则从 `CLAUDE.md` 里整段移除，写进 `AGENTS.md`（`AGENTS.md` 不存在就新建），然后在 `CLAUDE.md` 原来那四条规则的位置换成一行 `@AGENTS.md`。`CLAUDE.md` 里其余内容（项目结构说明、编码约定等等，凡是跟这四条规则无关的）原样保留，只搬走这四条规则本身。

不管规则原来在哪，处理原则不变：找到概念相同的旧规则，**替换已确认的治理部分**（纯治理 bullet 可以整体替换；混合 bullet 按上文先拆分），不要把新规则追加在旧规则旁边——两条讲同一件事、但内容冲突的规则同时留在文件里，人和 AI 都会不知道该听哪条，实测中这正是"装了新规则但行为完全没变"的直接原因。

- 若确实没有任何相关旧规则、是一个全新的规则区块，就在 `AGENTS.md` 末尾新增（标题按目标语言取名，比如"## 强制命令"或"## Rules"）。
- **除了这四条规则本身，不要删除或改写 `AGENTS.md`/`CLAUDE.md` 里任何其他已有内容**——这是在别人的项目里做加法/替换特定几行，不是全面重写。
- 仅采用 Claude Code 导入方案时，**务必确认 `CLAUDE.md` 顶部有 `@AGENTS.md` 这一行**——这是让 Claude Code 拿到规则的唯一机制，漏了这一行，`AGENTS.md` 写得再好 Claude Code 也读不到。`CLAUDE.md` 不存在就新建一个只含这一行的文件；已存在但没这行就加在文件最前面（新起一行，不要跟已有内容挤在一起）；已经有就不重复加。
- `AGENTS.md` 顶部如果还没有到 `docs/specs/`、`docs/tasks/`、`docs/HANDOFF.md`、`docs/WORKFLOW.md` 的导航链接，顺手加几行。

### AGENTS.md / CLAUDE.md 收尾自查——不要跳过，这一步专门用来抓"看起来做完了但漏了一条"

四条规则全部处理完后：

1. **回头搜索 `AGENTS.md`**，逐项核对下面这些旧规则的典型措辞是否属于未经确认的残留（用户已明确决定保留的项目约定除外）：
   - "实质性改动"（应该已经被"任何进入 git 提交的源码改动"这类可机械验证的措辞取代）
   - 提到要"覆盖"式维护一份状态文档，尤其是名字类似 `PROJECT_STATUS.md` 的文件
   - 按"改动规模/耗时"而非"验证复杂度"描述立项判据
2. **采用 Claude Code 导入方案时，回头搜索 `CLAUDE.md`**，确认（明确仅使用 AGENTS.md 的项目记为“不适用”，不新建 CLAUDE.md）：
   - 这四条规则的旧版本**没有残留在这里**（如果是迁移场景，它们应该已经被搬空、只剩一行 `@AGENTS.md`）
   - `@AGENTS.md` 这一行**确实存在**，而且在文件里能被读到的位置（不是被写进代码块或注释里）

**命中未经确认的旧规则，或采用导入方案却遗漏导入，说明处理没做完整**，回去补上，不要在没核对这一步之前就汇报"已完成"。用户已明确保留的旧约定应按该决定协调入口规则与模板，并在汇报列出保留项和原因；不能为了消除命中而删除已获确认的约定。这个自查本身就是本 skill 存在的意义——一次真实执行中，四条规则替换对了三条、漏了"交接日志规则"这一条，结果是 `docs/PROJECT_STATUS.md` 继续被维护、`docs/HANDOFF.md` 的新记录继续按旧习惯追加在文件中部，而且没有任何提示——直到几天后用户自己翻文件才发现。事后来看，一句 grep 就能当场抓到，不需要等人工发现。

## 步骤 4：自检与启用状态核验

全部文件写完后运行：

```bash
python3 tools/doc_symbols.py
python3 tools/task_pairs.py
```

两者 0 表示未发现问题或没有检查对象，1 表示疑点/缺失/待人工确认，2 表示配置或读取错误。记录统计和 `NO_OBJECTS`；空规格目录只能证明脚本能运行，不能声称规格通过验证。接口和词法检查边界见 `docs/WORKFLOW.md` A5/A6。

**必须在隔离临时 Git 仓库实测实际生成的 hook**：复制生成版本、启用临时仓库的 `core.hooksPath`，设置仅限临时仓库的测试身份，确认源码增改、删除、移入/移出匹配范围的重命名缺日志均拒绝；源码配暂存日志更新放行，仅文档变更放行，仅工作区未暂存日志不能放行。记录命令、退出状态和结果，完成后清理本次临时目录。无法运行时报告未验收。不要在目标仓库构造改动、暂存、提交或撤销测试，避免误提交用户已有暂存内容。

**随后单独核验目标仓库启用状态**：读取有效 `core.hooksPath` 与执行权限，区分文件已生成、目标配置已启用、隔离实测通过。仅在初始化授权明确包括仓库级配置且无冲突时执行 `git config --local core.hooksPath .githooks` 并回读；否则交付待执行命令并标明未启用。已有其他 hooksPath 时先确认整合方案。目标仓库不做真实测试提交，隔离测试通过不等于目标环境完整验收。

## 步骤 5：汇报给用户

清楚列出：

1. 创建了哪些文件、跳过了哪些（及原因）
2. **`AGENTS.md` 的四条规则分别是"新建""替换了旧版本"还是"从 CLAUDE.md 迁移过来的"**——逐条列，不要笼统说"已更新"。采用 Claude Code 导入方案时，确认 `CLAUDE.md` 顶部的 `@AGENTS.md` 导入行是新加的还是本来就有；明确仅使用 AGENTS.md 的项目列为“不适用，保留项目决定”。如果收尾自查有任何命中（旧措辞残留、或 `@AGENTS.md` 那行漏加），**必须在这里明确报出来**，不要因为大部分工作做完了就把这个当成小问题略过——这正是真实项目里出过的问题：三条改对了、一条漏了，当时的汇报没有提醒任何人去核对第四条
3. `docs/HANDOFF.md` 是新建、直接复用了匹配的旧文件、还是发现了格式冲突——冲突的话，用户当时给的决定是什么，要在这里复述一遍，避免以后忘记当初为什么这份文件长这样
4. 有没有发现类似 `PROJECT_STATUS.md` 的遗留状态文档，以及用户的处理意见
5. 目标仓库有效 hooksPath、执行权限及启用状态；若未启用，提供 `git config --local core.hooksPath .githooks` 并注明每个克隆单独配置、已有其他路径须先整合
6. 隔离 hook 实测结果、检查器统计与退出码；明确目标仓库未做真实提交验收，列出模板兼容性冲突和未完成项
7. **提醒用户**：如果他们同时用 Codex 等其他工具改这个项目，那些工具能不能真的吃到 `AGENTS.md` 里的规则，取决于那个工具本身支不支持这个约定——本 skill 只保证文件写对了地方，不能替用户验证每一款工具的行为，建议用户自己在那个工具里实测一次（比如故意漏写日志改一次代码，看看那个工具会不会照着规则提醒，已启用且未绕过的 git hook 会检查源码变更是否配日志文件更新，但不能判断日志内容）
8. 下一步：若 `docs/specs/` 尚无实际规格，按实际开发需要逐步撰写；第一次有需要锁死的具体参数时再建 `tests/test_frozen_decisions.py`

## 已知边界（诚实地告诉用户）

- 开发仓库、已安装副本与项目生成文件不自动同步；已有项目可先执行只读升级预览，不自动升级。已有模板先检查兼容性再复用；冲突要报告并确认处置，解决前不能写入与保留模板不兼容的新规则。`AGENTS.md` / `CLAUDE.md` 的替换或迁移同样受此约束。
- 如果目标仓库已经有一套不同的文档习惯（比如已有的 CHANGELOG.md、ADR 目录），本 skill 不会自动去整合或替代它们——初始化前应该和用户确认这些是否需要共存、合并或迁移，不要默认覆盖用户已有的工作方式。
- Codex 等其他工具是否真的会读 `AGENTS.md`、读了之后是否真的照做，本 skill 无法替用户验证——这依赖那个工具自身的实现，只能建议用户自己实测。git pre-commit 的执行不依赖 AI 配合，但取决于各克隆启用状态，且可被绕过；它只检查暂存源码变更是否配 HANDOFF 文件更新，不能证明日志内容正确。符号检查也不能证明规格语义。hook 按行匹配常规路径，不承诺包含换行等特殊文件名的完整覆盖。
