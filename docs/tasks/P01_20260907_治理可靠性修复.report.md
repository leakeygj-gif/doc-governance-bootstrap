# 治理可靠性修复验证报告

**日期**：2026-09-07
**项目**：P01 治理可靠性修复
**对应计划**：[治理可靠性修复](P01_20260907_治理可靠性修复.plan.md)
**性质**：本地代码与规则修改、隔离 Git 行为测试、只读流程推演；未操作硬件或外部系统。
**定稿状态**：已定稿
**结论**：本轮已修复的机械检查通过正常与异常场景验证，规则冲突经独立复核闭环；当前克隆未启用 hook，未完成真实目标项目安装验收。

## 1. 数据来源与前提

- 基线为 `main` 的 `ea846f8aaf626ec878ae9c2f4743331c6a773c59`，开工前工作区干净；结果针对本轮未提交的工作区文件。
- 运行环境：macOS，Python 3.14.0，Git 2.50.1（Apple Git-155）；没有安装新依赖。
- 行为夹具来源为仓库内 [tests/test_governance.py](../../tests/test_governance.py)。数据均为测试自行构造，不是用户项目或真实业务数据。
- 测试使用自动清理的临时目录与临时 Git 仓库，禁用测试进程的系统/全局 Git 配置，并在临时仓库配置测试身份和 hook。真实 commit 仅用于这些临时夹具。
- 关键文件 SHA-256（便于定位本次被测实现）：

| 文件 | SHA-256 |
|---|---|
| `tools/doc_symbols.py` | `d6862516dd4c861fd5dd9b1327c8ba7298b61871d2f20e67222c8565385a14b7` |
| `tools/task_pairs.py` | `7d9eaa8f6781fc3413375f3273036f3434a24883545f32a0807f49b2cda090bf` |
| `assets/templates/pre-commit` | `0cfbd26c522a89c5a1de2e309cf5b422ba1488f7d7034b99e110e47cc5e25525` |
| `tests/test_governance.py` | `d62f5fbc6a15ce65820a88a0fdc44f3d29d711412fb6f1b826fa0995754a3526` |

## 2. 方法

在仓库根目录运行：

```bash
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -v
python3 -B tools/doc_symbols.py
python3 -B tools/task_pairs.py
sh -n .githooks/pre-commit
sh -n assets/templates/pre-commit
git diff --check
```

另运行本机 skill-creator 附带的 `quick_validate.py` 对当前技能进行结构校验；其路径属于开发环境，不构成目标仓库运行依赖。

工具 worker 执行实现与初测，主对话抽查代码后独立重跑整套行为测试。测试比较真实提交放行/拒绝、检查器退出码和统计输出；模板与本仓库工具除配置项外做内容一致性校验。

独立审阅者在不知道前轮发现的前提下先制定两个用户场景，再只读推演新版指令：已有自定义 hook、旧文档约定和暂存业务修复的仓库；尚无文档与自定义 hook 的新仓库。该部分验证的是指令流程，不是实际安装行为。

## 3. 实测数据

主对话最终工具行为测试：**8 组全部通过，用时 4.615 秒**。其中的多场景子测试如下：

| 对象 | 输入/场景 | 观察结果 |
|---|---|---|
| hook | 新增、修改、删除源码，移出/移入源码匹配范围；未配日志 | 5 类真实临时提交均拒绝 |
| hook | 日志只在工作区修改未暂存；日志被删除 | 2 类真实临时提交均拒绝 |
| hook | 源码配暂存日志更新；仅文档改动 | 2 类真实临时提交均放行 |
| hook 配置 | 空匹配规则、未替换占位符、非法正则 | 均报告配置错误，hook 退出 2 |
| 符号词法 | 两个源码目录；真实标识符、较长标识符的子串、注释词、字符串词 | 2 源码文件、1 规格、4 引用、3 疑点，退出 1；模板与部署版结果一致 |
| 符号边界 | README、嵌套规格、代码围栏、正常引用 | README/围栏不计引用，嵌套规格纳入，正常引用退出 0 |
| 符号无对象/错误 | 仅说明页；缺源码目录；没有匹配后缀文件 | 分别为 `NO_OBJECTS`/0、错误/2、错误/2 |
| Task 配对 | 同项目不同阶段，第二阶段已完成却没有对应报告 | 不被第一阶段报告掩盖，退出 1 |
| Task 状态 | 执行中无报告；未知状态；无计划的报告 | 分别退出 0、1、1 |
| Task 复测 | 显式链接原计划；链接不存在；仅有复测而缺基线报告 | 分别退出 0、1、1；复测不能掩盖基线缺失 |
| Task 无对象/错误 | 空任务目录；目录缺失 | 分别 `NO_OBJECTS`/0、错误/2 |
| 模板一致性 | 两份 hook、两份符号工具、两份 Task 工具 | 配置差异以外内容一致 |

当前仓库符号检查实际为 **4 源码文件、0 规格文件、0 引用、0 疑点，输出 `NO_OBJECTS`**。这不证明实际规格正确，本轮仍未新增业务规格文档。

两份 hook 的 shell 语法检查、技能结构校验及 diff 空白检查均通过。

独立流程推演发现并闭环两项规则冲突：WORKFLOW 巡检仍禁止必要规格值；SKILL 自查可能删除用户已确认保留的状态文档约定。修正后审阅者定点复核通过，两个场景均要求协调既有配置、保护暂存内容、分开报告生成/启用/隔离实测。

边界核验：收口前 `.git/config` 与 `.git/index` 的 SHA-256 和开工记录一致，HEAD 仍为基线，暂存区为空。当前克隆没有配置 `core.hooksPath`，本轮没有启用它。

## 4. 本次算不了的指标与未验收边界

- 未在其他真实项目执行一次完整技能安装或升级；既有模板兼容性流程为人工检查与指令推演，没有通用自动迁移引擎。
- 未在当前克隆执行真实提交验收；临时仓库结果不代表用户所有机器、Git 客户端或 hook 组合都已验证。
- 词法检查不验证限定名归属、定义位置或规格语义；非 Python 语言的注释、字符串仍可能命中。没有进行跨语言语义准确率测量。
- hook 只检查暂存文件变化，不能判断日志是否对应本次改动；按行处理常规文件名，含换行等特殊路径不保证完整覆盖。
- Task 配对不证明报告内容真实性；复测关联要求同目录计划文件及独立的 `**对应计划**` Markdown 链接行。
- 没有验证多种 AI 工具对规则的实际加载效果、非中文初始化或多操作系统运行；机器字段保留策略已经写入技能，但未进行翻译安装实测。

## 5. 结论

本轮修复完成：删除与跨范围重命名纳入守卫，配置错误不再静默放行；Task 按完整前缀检查并允许显式关联的复测报告；符号检查输出可区分无对象、疑点和错误。规则与模板统一了兼容性处置、证据抽查、计划受控修订及必要规格值来源。

修改没有触碰已批准禁区，原有历史交接条目保持原样。当前成果为本仓库未提交的开发修改；已安装 skill 未更新。

## 6. 建议

后续若要用于具体项目，按更新后的流程核对该项目的既有文档和 hook，确认启用范围，再分别记录文件生成、配置启用与隔离测试结果。无需为本轮开发新增依赖。
