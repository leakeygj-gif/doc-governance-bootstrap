# 公开前隐私清理 — 发布阶段报告

**日期**：2026-09-07
**项目**：P05 公开前隐私清理
**对应计划**：[总计划](plan.md)
**状态**：本阶段记录定稿；整体任务阻塞，仓库保持 PRIVATE。

## 数据来源与前提
承接已通过主审的[文件阶段报告](report.md)，用户授权不备份、替换同一远端历史后公开。发布前远端只有默认分支 main，无标签。原始敏感正文、旧提交编号及个人邮箱不再次写入本报告。

## 方法
显式暂存 19 个预期路径（17 个跟踪文件改删、2 个新增计划与报告），手动运行现有 pre-commit 和暂存差异空白检查。core.hooksPath 尚未配置；本次手动检查不代表 hook 已启用。
使用 write-tree、无父 commit-tree 及 update-ref 建立独立根提交，作者和提交者采用账户公开 noreply 身份，未修改全局配置。扫描根提交文件树并验证身份。重新读取远端基准，使用精确 force-with-lease 只推送 main；未使用 mirror。
替换后检查远端分支、标签、旧提交 API 及旧敏感文件 contents API；响应正文只在检查进程内判断，不落盘、不打印。旧对象可访问即停止公开。

## 实测数据
| 检查 | 结果 |
|---|---|
| 预期暂存路径 | 19，手动 hook 与 diff 空白检查通过 |
| 独立根提交 | 94e77e0591e4d4abb4c62b8d5e7cd479a2b5de49；无父提交，历史计数 1 |
| 根提交身份与文件树 | 作者及提交者均为公开 noreply；47 文件已识别隐私与常见凭据模式零命中 |
| 远端并发检查与替换 | 基准一致，精确 lease 推送成功；远端 main 等于新根提交，公开分支与标签引用只有 main |
| 旧提交 API | HTTP 200，返回匹配的旧提交 |
| 旧敏感文件 API | HTTP 200，返回内容匹配已识别隐私标识 |
| 仓库可见性 | PRIVATE；未调用公开操作，未进行匿名公开验收 |

## 规格同步结果
无需同步：本阶段仅清理发布历史、检查托管状态并记录门禁结果，不改变工具接口、契约或功能；没有对应规格需要更新。

## 本次算不了的
分支历史替换不能清除 GitHub 保留的旧对象；本次没有删除或重建远端仓库、联系支持、安装依赖或创建备份。本地应用管理的 refs/codex 引用及既有 reflog 未销毁，因此不能宣称所有本地旧对象已消失。仅将 main 推送到远端，没有推送应用引用。没有证明不存在未知秘密或其他缓存。

## 结论与建议
公开文件树及远端 main 历史已清理，但旧敏感内容仍可通过 GitHub API 访问，未通过公开前隐私门禁，仓库保持私有。下一步需用户确认托管方旧对象清除方案；清除后重新核验并另写新报告，不能直接切换公开。此阶段报告及状态记录以公开身份的干净后代提交同步，不引入旧历史。
