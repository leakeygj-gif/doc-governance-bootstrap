# 治理可靠性修复

**创建日期**：2026-09-07
**状态**：已完成
**项目**：P01 治理可靠性修复
**关联 worker**：`/root/tool_fixes`（工具与行为测试）、`/root/doc_fixes`（规则与模板文档）、`/root/independent_review`（独立流程推演）
**基准**：main / ea846f8aaf626ec878ae9c2f4743331c6a773c59；开工前工作区干净。
**授权**：用户已确认本轮具体修改范围、隔离测试与文档交付。

## 目标

修复文档治理初始化、源码日志守卫、Task 配对及规格符号检查的已知可靠性缺口，统一相关规则与模板。保持初始化工具的定位，并如实区分机械检查与人工判断。

## 边界

- 工具 worker：`assets/templates/pre-commit`、`.githooks/pre-commit`、`assets/templates/doc_symbols.py`、`tools/doc_symbols.py`；允许在 `tools/` 及 `assets/templates/` 添加必要的 Task 配对检查器、在 `tests/` 添加标准库行为回归测试。
- 文档 worker：`SKILL.md`、`README.md`、`AGENTS.md`、`references/methodology.md`、`assets/templates/AGENTS-rules-block.md`、`assets/templates/WORKFLOW.md`、`assets/templates/tasks-README.md`、`assets/templates/specs-README.md`、`assets/templates/HANDOFF.md`、`docs/WORKFLOW.md`、`docs/specs/README.md`、`docs/tasks/README.md` 的规则及模板部分。
- 主对话：本 plan、对应 report、`docs/HANDOFF.md` 和项目登记表收口；协调验收、按风险抽查证据。不得与 worker 同时编辑同一文件。
- 临时 Git 仓库及测试数据由测试创建与清理；其中允许本地配置、暂存、提交，用于验证真实 hook 行为。

## 禁区

- 不安装依赖，不修改当前仓库的 Git 配置、暂存区、分支或提交历史，不提交、不推送。
- 不更新用户目录里已安装的 skill，不修改其他项目，不触发网络真实写入、生产操作或硬件动作。
- 不覆写 HANDOFF 历史条目；本次只调整说明区并追加对应记录。
- 不实现通用自动模板升级，不把词法符号检查宣称为语义验证或完整审阅器。
- 不因测试失败而降低验收要求；若目标、范围或验收标准需要改变，记录原因并重新取得用户确认。

## 分步计划

1. 主对话先落盘已确认 plan，再分派互不重叠的文件范围。
2. 工具 worker 修复日志守卫，覆盖删除与跨匹配范围重命名；守卫仅保证日志文件变化，不承诺日志内容正确。补充完整前缀 Task 配对及符号检查的多目录、标识符匹配、统计与明确的结果状态。
3. 文档 worker 统一启用/验证/汇报顺序；采用隔离仓库验证 hook，保护现有 hooksPath；检查已有模板兼容性并报告冲突，不静默替换已有工作流。明确证据抽查、受控计划修订、必要规格值的权威引用。
4. 将工具接口与模板命令交叉核对，运行有正常与异常输入的行为测试；独立审阅者检查真实用例和规则一致性，不写生产文件。
5. 主对话依据真实结果写 report，追加 HANDOFF，更新 plan 与登记表；最终核对 diff、工作区和配置未越界。

## 验收点

- [x] 隔离 Git 仓库实测：普通源码增改、删除、重命名且缺少 HANDOFF 更新被拒；配有日志更新正常放行；仅文档改动放行；仅未暂存的日志不能放行。
- [x] Task 检查按完整前缀匹配；同项目另一阶段或另一轮报告不能掩盖缺失；执行中无 report 正常，空任务目录明确说明无对象。
- [x] 符号检查遍历所有配置源码目录、避免较长标识符子串误匹配；报告扫描文件数、引用数、疑点数；无对象、疑点、配置错误的语义与退出码有文档及测试。保留词法方法的诚实边界。
- [x] 初始化说明区分文件生成、配置启用、隔离实测与目标仓库未验收；已有模板发生冲突不能报告完整安装成功。
- [x] 主代理可抽查证据；计划变更需原因和确认；必要固定值有权威引用/自动生成路径，不再一概禁止。
- [x] 模板和本仓库对应工具、规则保持一致（项目定制项除外）；技能结构校验、相关行为测试、`git diff --check` 通过或明确记录环境限制。
- [x] 新证据在 report 只写一次，HANDOFF 引用 report；当前仓库未暂存、未提交、Git 配置未修改。

## 风险预判

- 词法检查仍不能证明类归属、运行时行为或规格语义正确；非 Python 源码中的注释等需明确说明能力边界。
- 本地 hook 依赖每个克隆的启用状态，也可被绕过；本任务不启用当前克隆。
- 模板兼容性涉及人工判断，本轮提供检查与冲突处置流程，不构建通用迁移引擎。
- 历史记录保持原样，其中旧行为描述仅代表当时状态，以本轮 report 和当前规则说明变化。
