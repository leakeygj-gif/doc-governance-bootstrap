# HANDOFF.md — 交接日志（追加式）

> 用途：任何进入 git 提交的源码改动，提交前都要在下方记录区顶部追加对应交接记录；巡检产生的改动也要记录。
> 换会话/换模型后，新接手者读最近 1-3 条即可续上。
>
> 如果这个项目在装这套体系之前已经有自己的开发日志、格式跟这里不兼容（比如按时间从旧到新往下追加、自由格式而不是下面按有无 Task 分档的模板），**不要另存一个归档文件**——把旧内容原样接到本文件最下方，中间插入这行固定文字（逐字复用，`docs/WORKFLOW.md` 的 A4 巡检脚本靠它判断日期序检查该在哪里停下）：
> ```
> ---
>
> <!-- 归档分界：以下内容归档自旧格式历史记录，不适用上方"新记录插最上面/五段结构"的约定 -->
>
> ## 历史记录（<旧文件的日期范围>，旧格式，仅供追溯，不再追加）
> ```
> 归档段落本身不做任何整理或重写，原样保留；新记录一律只加在本文件最上方、这条分界线以上。

---

## 模板

有 Task：只记结论与原因，详细方法和数据放在 report 一处，日志引用它。

```markdown
## <YYYY-MM-DD HH:MM> — <任务简称>
- **改了什么**：1～2 句结论
- **为什么**：
- **风险/未完成**：
- **报告**：[验证报告](tasks/<...>.report.md)
```

无 Task：保留完整五段。若验证需要表格、多组数据或方法说明，应先立 Task，不把详细证据塞进日志。

```markdown
## <YYYY-MM-DD HH:MM> — <任务简称>
- **改了什么**：
- **为什么**：
- **验证**：实际验证结果；规格同步结果：已同步（链接与要点）／无需同步（具体原因）／待同步（缺项与下一步）
- **下一步**：
- **风险/未完成**：
```

两档都完整保留「为什么」。有 Task 的规格同步结果只写 report，HANDOFF 引用报告；无 Task 写在上方「验证」段。待同步不得宣称全部收口。

---

## 历史记录

<!-- 在这条注释下方追加新记录，最新的在最上面 -->

## 2026-09-07 18:00 — 公开前隐私清理（文件阶段）
- **改了什么**：移除外部项目真实验证资料及关联敏感内容，公开版历史报告明确标注撤下或脱敏；保留通用工具与测试。
- **为什么**：真实私有样本不是技能分发所必需，避免公开仓库泄露其他项目的研发与验收情况。
- **风险/未完成**：不备份，部分历史证据不可恢复；本条仅记录文件阶段，远端历史替换与公开核验由发布阶段另行记录。
- **报告**：[P05 文件阶段报告](tasks/P05_20260907_公开前隐私清理/report.md)

> 公开版说明：以下历史摘要中的外部项目标识已泛化。P02/P03 原报告及部分资料已因隐私清理撤下，其链接现指向明确的撤下说明；历史“保留原证据”等描述仅代表当时状态。

## 2026-09-07 17:23 — 规格同步结果留痕
- **改了什么**：规则与 report/HANDOFF 模板要求明确记录已同步、无需同步或待同步，收口审阅核对依据；待同步不能标全部完成。
- **为什么**：区分检查后无需更新与遗漏检查，减少代码修改后主规格静默过时。
- **验证**：规则、模板与工作流一致性复核，skill结构及diff检查通过；规格同步结果：无需同步，本次修改治理流程和记录模板，docs/specs 当前仅说明页、没有对应实际规格，规则来源已直接同步 AGENTS 与 WORKFLOW。
- **下一步**：提交后按需同步安装副本，再用于项目升级。
- **风险/未完成**：未增加自动拦截；记录内容仍需人工审阅。安装副本和外部项目未更新，历史报告不追补。

## 2026-09-07 16:39 — 新任务集中保存
- **改了什么**：新任务将计划、报告和专用资料集中到一个目录，检查器兼容旧平铺并区分同名文件，源码守卫覆盖任务内脚本。
- **为什么**：减少寻找资料和选择分类的负担，保留实际复用的公共工具，避免目录变化引入漏检。
- **风险/未完成**：历史资料未迁移，外部项目及安装副本未升级；当前仅本地保存，未提交或验证外部归档。
- **报告**：[P04报告](tasks/P04_20260907_任务集中目录/report.md)

## 2026-09-07 16:22 — 修复索引漏检并验证证据保护
- **改了什么**：补查索引中的失效计划链接；新复核脚本按轮次保存并拒绝覆盖，修正只读巡检与纯 AGENTS 例外的局部措辞。
- **为什么**：避免把断链当无对象、复测覆盖旧证据，以及局部执行规则引发未经授权的项目写入。
- **风险/未完成**：P02原脚本保留作历史依据，不应重跑覆盖；项目升级和外部备份未执行。共享工具历史版本见P03快照。2026-09-07 公开清理修订：原样本与原报告已撤下，详见下方报告链接的公开说明。
- **报告**：[P03报告](tasks/P03_20260907_漏检与证据保护验证.report.md)

## 2026-09-07 16:07 — 关联任务资料并兼容阶段报告
- **改了什么**：任务专用脚本和资料目录沿用 plan/report 前缀，报告提供资料入口；检查器支持状态边界、显式阶段关联和有来源的精确历史豁免。
- **为什么**：方便辨认分析资料归属，减少旧任务格式误报，同时保留真正缺失与需人工确认的问题；明确开发仓库、安装副本和项目文件不自动同步。
- **风险/未完成**：外部项目与已安装 skill 未更新；只读样本仍有待核查项，资料目前仅本地保存，未提交或验证外部归档。2026-09-07 公开清理修订：原报告与真实样本已移除，详见下方报告链接的公开说明。
- **报告**：[P02 验证报告](tasks/P02_20260907_任务资料关联与兼容检查.report.md)

## 2026-09-07 10:31 — 修复治理检查与初始化验收缺口
- **改了什么**：补齐源码日志守卫、Task 配对和符号检查，统一初始化兼容性处置、启用状态说明及规则模板。
- **为什么**：避免删除源码和同项目多阶段任务漏检、空检查被误认为验收通过，以及新入口规则与旧工作流冲突；同时保留用户已有约定和可复核的证据。
- **风险/未完成**：当前克隆 hook 未启用，已安装 skill 未更新；未进行真实目标项目安装或提交验收，检查工具的能力边界见报告。
- **报告**：[P01 验证报告](tasks/P01_20260907_治理可靠性修复.report.md)

## 2026-09-03 22:26 — 首次接入 GitHub 远端，补齐 README / LICENSE / .gitignore
- **改了什么**：新增面向人的 `README.md`（不是 `SKILL.md` 的复述，只讲"解决什么问题 / 装完得到什么 / 怎么用 / 刻意的设计取舍 / 已知边界"）、`LICENSE`（MIT）、`.gitignore`（macOS + Python + 编辑器临时文件）；为本仓库配置 GitHub 私有远端并推送既有历史。
- **为什么**：此前只有一个本地基线提交、没有任何 remote，全部内容只存在于一台机器上——这与上一条记录立下的"从此往后的改动都有真实、可回退的历史"直接矛盾，一次磁盘故障就会让自举的意义归零。README 单独写而不复用 `SKILL.md`：后者是给 AI 读的执行步骤（含大量"这一步容易漏做"的自查指令），当门面会让人读不到重点。仓库设为 private：`AGENTS.md` 的称呼规则和 `references/methodology.md` 里含个人工作习惯，公开后对外部使用者是噪音；private 以后随时能改 public，反向改不回来。
- **验证**：`python3 tools/doc_symbols.py` 通过；三个新增文件都不在 `.githooks/pre-commit` 的 `SRC_PATTERN`（`^(SKILL\.md|references/.*\.md|assets/templates/.*)$`）内，本次提交不会被 hook 拦截——仍写本条记录，是因为这是需要留痕的仓库级变更，而非 hook 强制；README 中引用的 `references/methodology.md` 等路径已逐一核对存在。
- **下一步**：① **上一条记录的开放问题②已结案：不新建"协作纪律"skill**——理由是这三条规则大部分已有更成熟的承载物：「写入前确认」由 Claude Code 原生 Plan Mode 承担（harness 层的模式强制，比写在 `AGENTS.md` 里指望模型自觉可靠），「不写兼容代码」已是默认系统提示词的既有行为，剩下「决策边界」「称呼」两条留在个人全局 `CLAUDE.md` 即可，不值得为它们单开一个 skill；② 开放问题①（`CLAUDE.md` 是否整体迁入 `AGENTS.md`）仍未决；③ 调研了同类方案（OpenSpec / GitHub Spec Kit / adr-kit / Flagrare staleness-audit），其中 adr-kit 有两个机制值得借鉴——**规格文档内嵌可机读断言**（`forbid_pattern` / `require_pattern` + `path_glob`，commit 时确定性校验，断言住在规格内部因而不会与规格漂移）、**巡检分两级成本档并持久化趋势状态**（便宜档确定性脚本自动跑，贵档需模型判断、先问过再花钱）。这两条正对着本体系目前最弱的"规格同步"一环，尚未立项。
- **风险/未完成**：README 里对同类方案的取舍表述基于 2026-09-03 的一次网络调研，未来这些工具演进后可能失准；`docs/specs/` 至今为空，`tools/doc_symbols.py` 的"通过"目前只证明脚本能跑，没有真正校验过任何符号。
- **关联 PLAN**：无（验证一句话说得清，按现行规则不立 Task）

## 2026-08-30 — doc-governance-bootstrap 从外部项目的讨论中独立成项目
- **改了什么**：建立 git 仓库；对本目录跑一遍我们自己的体系（自举）——`docs/specs/`、`docs/tasks/`、`docs/refs/`、`docs/HANDOFF.md`、`docs/WORKFLOW.md`、`.githooks/pre-commit`（源码匹配规则是 `SKILL.md`、`references/`、`assets/templates/` 的改动）、`tools/doc_symbols.py`；新建 `AGENTS.md`（四条规则 + 称呼规则）与 `CLAUDE.md`（`@AGENTS.md` 导入）。
- **为什么**：这个 skill 是在 外部项目的会话里、围绕另一件事的讨论中逐步长出来的，讨论已经深入到需要独立空间——一次性交接文档会像 `PROJECT_STATUS.md` 一样很快过时，所以选择把这个目录本身变成一个有 git 历史、有交接日志、能自己保持新鲜的项目，而不是写一份终将过时的说明书。
- **验证**：`.githooks/pre-commit` 源码匹配正则对 `SKILL.md`/`references/*.md`/`assets/templates/*` 实测命中，对 `docs/*` 不命中（避免"改文档要求写文档说明改了文档"的循环）；`tools/doc_symbols.py` 因 `docs/specs/` 尚无内容文档，预期直接通过。
- **下一步**：两个开放问题尚未决定，是这个新项目的第一批待办——① `CLAUDE.md` 的内容要不要整体迁移进 `AGENTS.md`（不止四条规则，项目结构/常用命令/架构要点这些同样不是 Claude 专属）；② 决策规则/代码兼容性/代码改动规则这三条通用的协作纪律，要不要独立成一个新 skill（不并入 `doc-governance-bootstrap`，因为这是两件不同的事：一个防文档腐烂，一个管 AI 自主行动的边界）。
- **风险/未完成**：本条记录之前没有任何 git 历史，`references/methodology.md` 里记录的设计决定过程是从对话记忆整理的，不是从提交历史回溯的，细节的准确性依赖记忆而非可复核的记录——这是本次独立成项目要解决的问题本身：从这条记录起，往后的改动都会有真实的、可回退的历史。
- **关联 PLAN**：无（验证一句话说得清，按现行规则不立 Task）
